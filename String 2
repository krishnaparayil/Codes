include <stdio.h>

void main()
{
int v=0;
char str[50];
char ch;
int i;
printf("enter the string:");
scanf("%s",str);
for(i=0;str[i]!='\0';i++)
{
ch=str[i];
if(ch=='a'||ch=='e'||ch=='i'||ch=='o'||ch=='u')
 v++;
 }
printf("%d",v);
}
